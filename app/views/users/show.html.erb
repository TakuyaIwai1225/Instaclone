<div class="row">
  <div class="col-md-6 col-md-offset-3">
    <h1><%= @user.name %>のページ</h1>
    <p>メールアドレス: <%= @user.email %></p>
    <% @user = current_user %>
    <p>
      <strong>Your Profile Image:</strong>
      <%= image_tag @user.image if @user.image %>
    </p>
    <%= link_to "さっそくログインする", new_session_path  if current_user.blank? %>
    <%= link_to "ユーザー情報を編集する", edit_user_path %>
  </div>
</div>